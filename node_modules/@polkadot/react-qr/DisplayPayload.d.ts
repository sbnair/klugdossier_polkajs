import React from 'react';
interface Props {
    address: string;
    className?: string;
    cmd: number;
    genesisHash: Uint8Array | string;
    payload: Uint8Array;
    size?: string | number;
    style?: React.CSSProperties;
}
declare function DisplayPayload({ address, className, cmd, genesisHash, payload, size, style }: Props): React.ReactElement<Props> | null;
export declare const QrDisplayPayload: React.MemoExoticComponent<typeof DisplayPayload>;
export {};
