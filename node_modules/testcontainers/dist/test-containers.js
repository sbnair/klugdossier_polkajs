"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestContainers = void 0;
const port_forwarder_1 = require("./port-forwarder");
const docker_client_instance_1 = require("./docker-client-instance");
class TestContainers {
    static exposeHostPorts(...ports) {
        return __awaiter(this, void 0, void 0, function* () {
            const portForwarder = yield port_forwarder_1.PortForwarderInstance.getInstance(yield docker_client_instance_1.DockerClientInstance.getInstance());
            yield Promise.all(ports.map((port) => portForwarder.exposeHostPort(port)));
        });
    }
}
exports.TestContainers = TestContainers;
