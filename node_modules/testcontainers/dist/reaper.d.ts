import { DockerClient } from "./docker-client";
export interface Reaper {
    addProject(projectName: string): void;
}
export declare class ReaperInstance {
    static IMAGE_NAME: string;
    static IMAGE_VERSION: string;
    private static instance;
    static getInstance(dockerClient: DockerClient): Promise<Reaper>;
    private static isEnabled;
    private static getImageName;
    private static getImageVersion;
    private static createDisabledInstance;
    private static createRealInstance;
}
