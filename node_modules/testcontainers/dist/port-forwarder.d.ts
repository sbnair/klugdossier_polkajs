import { SshConnection } from "ssh-remote-port-forward";
import { DockerClient } from "./docker-client";
import { Port } from "./port";
import { StartedTestContainer } from "./test-container";
export declare class PortForwarder {
    private readonly sshConnection;
    private readonly container;
    constructor(sshConnection: SshConnection, container: StartedTestContainer);
    exposeHostPort(port: Port): Promise<void>;
    getNetworkId(): string;
    getIpAddress(): string;
    private getNetworkName;
}
export declare class PortForwarderInstance {
    static IMAGE_NAME: string;
    static IMAGE_VERSION: string;
    private static instance;
    static isRunning(): boolean;
    private static getImageName;
    private static getImageVersion;
    static getInstance(dockerClient: DockerClient): Promise<PortForwarder>;
    private static createInstance;
}
