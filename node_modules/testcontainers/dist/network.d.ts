import { CreateNetworkOptions, DockerClient } from "./docker-client";
import { Uuid } from "./uuid";
export declare class Network {
    private readonly uuid;
    private readonly createNetworkOptions;
    constructor(createNetworkOptions?: Partial<CreateNetworkOptions>, uuid?: Uuid);
    start(): Promise<StartedNetwork>;
}
export declare class StartedNetwork {
    private readonly id;
    private readonly options;
    private readonly dockerClient;
    constructor(id: string, options: CreateNetworkOptions, dockerClient: DockerClient);
    getId(): string;
    getName(): string;
    stop(): Promise<StoppedNetwork>;
}
export declare class StoppedNetwork {
}
