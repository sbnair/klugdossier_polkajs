import { GenericContainer } from "../..";
import { Image, Tag } from "../../repo-tag";
import { StartedTestContainer } from "../../test-container";
import { Port } from "../../port";
import { AbstractStartedContainer } from "../abstract-started-container";
export declare class ArangoDBContainer extends GenericContainer {
    private password;
    private readonly defaultPort;
    private readonly defaultUsername;
    constructor(image?: Image, tag?: Tag, password?: string);
    withPassword(password: string): this;
    protected preCreate(): Promise<void>;
    start(): Promise<StartedArangoContainer>;
}
export declare class StartedArangoContainer extends AbstractStartedContainer {
    private readonly port;
    private readonly username;
    private readonly password;
    private readonly host;
    constructor(startedTestContainer: StartedTestContainer, port: Port, username: string, password: string);
    getTcpUrl(): string;
    getHttpUrl(): string;
    getPassword(): string;
    getUsername(): string;
}
