import { GenericContainer, StartedTestContainer } from "../..";
import { Image, Tag } from "../../repo-tag";
import { AbstractStartedContainer } from "../abstract-started-container";
import { Port } from "../../port";
export declare class ElasticsearchContainer extends GenericContainer {
    readonly image: Image;
    readonly tag: Tag;
    readonly timeout: number;
    private readonly httpPort;
    constructor(image?: Image, tag?: Tag, timeout?: number);
    start(): Promise<StartedElasticsearchContainer>;
}
export declare class StartedElasticsearchContainer extends AbstractStartedContainer {
    readonly startedTestContainer: StartedTestContainer;
    private readonly httpPort;
    constructor(startedTestContainer: StartedTestContainer, httpPort: Port);
    getHttpUrl(): string;
}
