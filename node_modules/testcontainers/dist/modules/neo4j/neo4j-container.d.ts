import { GenericContainer } from "../..";
import { Image, Tag } from "../../repo-tag";
import { StartedTestContainer } from "../../test-container";
import { Port } from "../../port";
import { AbstractStartedContainer } from "../abstract-started-container";
export declare class Neo4jContainer extends GenericContainer {
    private password;
    private readonly defaultBoltPort;
    private readonly defaultHttpPort;
    private readonly defaultUsername;
    constructor(image?: Image, tag?: Tag, password?: string);
    withPassword(password: string): this;
    withApoc(): this;
    withTtl(schedule?: number): this;
    protected preCreate(): Promise<void>;
    start(): Promise<StartedNeo4jContainer>;
}
export declare class StartedNeo4jContainer extends AbstractStartedContainer {
    private readonly username;
    private readonly password;
    private readonly host;
    private readonly boltPort;
    private readonly httpPort;
    constructor(startedTestContainer: StartedTestContainer, boltPort: Port, httpPort: Port, username: string, password: string);
    getBoltUri(): string;
    getHttpUri(): string;
    getPassword(): string;
    getUsername(): string;
}
