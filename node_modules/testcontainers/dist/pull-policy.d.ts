export interface PullPolicy {
    shouldPull(): boolean;
}
export declare class DefaultPullPolicy implements PullPolicy {
    shouldPull(): boolean;
}
export declare class AlwaysPullPolicy implements PullPolicy {
    shouldPull(): boolean;
}
