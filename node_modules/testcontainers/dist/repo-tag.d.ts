export declare type Image = string;
export declare type Tag = string;
export declare class RepoTag {
    private readonly image;
    private readonly tag;
    private readonly HELPER_CONTAINERS;
    constructor(image: Image, tag: Tag);
    equals(repoTag: RepoTag): boolean;
    toString(): string;
    isReaper(): boolean;
    isHelperContainer(): boolean;
    static fromString(string: string): RepoTag;
    private static fromImage;
}
